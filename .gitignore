.DS_Store
node_modules
/dist

# PWA (service worker gerado em dev pelo vite-plugin-pwa)
/dev-dist

# local env files
.env
.env.local
.env.*.local


# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Editor directories and files
.idea
.vscode
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# lockfile de outro gerenciador — o projeto usa Yarn Classic
package-lock.json

# config local de ferramentas de IA (a config de projeto E versionada)
.claude/settings.local.json

# build do Storybook (catálogo de componentes — REFACTOR_AUDIT_PLAN.md, Fase 3)
storybook-static
