# _revisar_

Código descartado durante a padronização/refatoração descrita em
`REFACTOR_AUDIT_PLAN.md`, preservado aqui para consulta futura — não é
importado por nenhuma página/componente ativo.

Exemplo: quando a Fase 1 escolhe um único header/footer canônico
(`src/components/UI/AppHeader/`, `src/components/UI/AppFooter/`), as
implementações não escolhidas (header da Landingpage, header do
`NewEventDetails`, footer do `FeedCard`/`FeedCardStandalone`, `AppFooter`
antigo) são movidas para cá, do jeito que estavam, em vez de apagadas.

Decisão de remoção definitiva do que estiver aqui fica para depois deste
esforço — ver seção "Princípios" do `REFACTOR_AUDIT_PLAN.md`.
